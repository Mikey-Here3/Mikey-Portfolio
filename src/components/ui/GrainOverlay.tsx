/**
 * GrainOverlay
 * Subtle film grain texture overlay for a cinematic feel.
 * Uses CSS SVG noise pattern — zero performance cost.
 */
export default function GrainOverlay() {
    return <div className="grain-overlay" aria-hidden="true" />;
}
